#ifndef TP2_THREAD_POOL_H
#define TP2_THREAD_POOL_H
/*
#include <thread>
#include "bounded_buffer.h"
#include "fs_track.h"

FS_Track blablabla(FS_Track data){
    return data;
}

template<typename t, size_t len, uint32_t threadNum>
class ThreadPool{
    private:
    std::thread threads[threadNum];

    public:
    BoundedBuffer<t, len> boundedBuffer;

    ThreadPool(void processData()) {
        for(const auto& thread : threads)  {
            thread(processData);
        }
    }

    ~ThreadPool() = default;

    void joinThreads(){
        for(const auto& t)
    }
};
*/
#endif //TP2_THREAD_POOL_H
